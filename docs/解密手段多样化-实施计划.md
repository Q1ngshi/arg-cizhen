# 解密手段多样化重构 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 四门四手段重构——linyuan 保留（教学门）/ 2009 改日期提取 / 1942 改计算器破译 / 0172 改图纸观察，线索分散到全游戏载体。

**Architecture:** index.html（官网伪装层）改卷宗落款与管理员提示 + dossier3 印证；desktop.html（终端层）改计算器破译检测、值班员引导、图纸铭牌、档案门密码。test-tracking.js 全流程断言同步。

**Tech Stack:** 纯 HTML/CSS/JS 单文件 ×2 + Playwright（本地 localhost:8081 验证）。

## Global Constraints

- 基调六红线（不跳吓/不血腥/不直白/不卖惨/不出戏/不嘲讽玩家）；"不卡壳到红温退出"——所有新线索必须有明示引导
- 所有 localStorage/sessionStorage 键不变，老玩家进度不受影响
- 埋点 key 命名空间与既有 logStage 一致；同 key 去重由 logStage 自身保证
- 测试环境：先 `cd /c/Users/32268/Desktop/arg-game`；server localhost:8081（serve.js）；`NODE_PATH=/c/Users/32268/AppData/Local/npm-cache/_npx/e41f203b7505f1fb/node_modules`；chromium `C:/Users/32268/AppData/Local/ms-playwright/chromium-1200/chrome-win64/chrome.exe`
- 每个 git commit 必须含 `Co-Authored-By: Claude <noreply@anthropic.com>`；提交前必须 cd 仓库目录
- 设计文档：`docs/解密手段多样化-重构设计.md`（已定稿，本计划的两处实现微调见 Task 3 说明，实施后回写设计文档）

---

### Task 1: 门② 日期提取（index.html）

**Files:**
- Modify: `C:\Users\32268\Desktop\arg-game\index.html`（dossier 卷宗落款 + admin-modal 提示文案）

**Interfaces:**
- Produces: 卷宗 CE-2009-005 末尾落款"二〇〇九年三月十七日"（汉字）；管理员门提示"密码：卷宗落款年份（4 位数字）"。不影响任何 JS 逻辑/埋点 key。

- [ ] **Step 1: 卷宗加汉字落款**

在 `index.html` 的 `#dossier` 内，"关联卷宗" `<ul class="dossier-link-list">` 区块结束 `</div>` 之后、"系统提示" `<div class="dossier-footer-hint">` 之前，插入：

```html
      <div class="dossier-section" style="margin-top:18px">
        <p style="text-align:right;color:#8a8a8a;font-size:13px;font-family:'SimSun','宋体',serif">
          慈恩镇档案管理办公室<br>二〇〇九年三月十七日
        </p>
      </div>
```

- [ ] **Step 2: 管理员门提示文案**

`index.html` 第 466 行附近（`#admin-modal` 内）：将

```html
    <div style="color:#666;font-size:11px;margin-top:6px">提示：账号格式见机构概况·内设机构；密码为该系列案件的归档年份（4位数字）</div>
```

改为：

```html
    <div style="color:#666;font-size:11px;margin-top:6px">提示：账号格式见机构概况·内设机构；密码：卷宗落款年份（4位数字）</div>
```

- [ ] **Step 3: 验证（Playwright 快速脚本）**

临时脚本（跑完即删，或复用 `verify-chat.js` 同款框架）：
1. goto `http://localhost:8081/`，清除 localStorage，reload
2. 搜索"林远"→ 点结果 → `#gate-input` 输 `linyuan` → 确认 → 等 `#dossier.show`
3. 断言 `#dossier` 文本含 `二〇〇九年三月十七日`
4. 关闭卷宗 → 搜索"槐树街" → 首页点新闻 → `#admin-modal` → 断言提示文本含 `卷宗落款年份`
5. 关闭

预期：两处断言通过。

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "门②日期提取：卷宗汉字落款 + 管理员口令提示指向落款年份

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 门③ 计算器破译（desktop.html）

**Files:**
- Modify: `C:\Users\32268\Desktop\arg-game\desktop.html`（calc 函数、CSS、MSG_TEXTS、pollEvents）

**Interfaces:**
- Produces: `calcBroken` 全局变量（防重复破译反馈）；`logStage('calc_break')`；`fireMsg('calc_break')`；事件 `cz_evt_gate3_open` → 值班员引导消息（fireIncoming）。MSG_TEXTS 新增 key：`calc_break`。

- [ ] **Step 1: calc() 加破译检测**

`desktop.html` `calc(k)` 函数（约 1003 行）的 `=` 分支，将

```js
  if (k === '=') {
    try {
      var r = Function('return (' + cExpr.replace(/÷/g,'/').replace(/×/g,'*').replace(/−/g,'-') + ')')();
      d.textContent = Math.round(r*1e8)/1e8;
      cExpr = String(d.textContent);
    } catch(e){ d.textContent = '错误'; cExpr=''; }
    return;
  }
```

改为：

```js
  if (k === '=') {
    try {
      var r = Function('return (' + cExpr.replace(/÷/g,'/').replace(/×/g,'*').replace(/−/g,'-') + ')')();
      var rr = Math.round(r*1e8)/1e8;
      d.textContent = rr;
      cExpr = String(rr);
      if (rr === 1942 && !calcBroken) {
        calcBroken = true;
        d.classList.remove('calc-break'); void d.offsetWidth; d.classList.add('calc-break');
        fireMsg('calc_break');
        logStage('calc_break');
      }
    } catch(e){ d.textContent = '错误'; cExpr=''; }
    return;
  }
```

并在 `cExpr` 变量声明处（搜索 `var cExpr`）旁边加：

```js
var calcBroken = false;
```

- [ ] **Step 2: 破译闪光 CSS**

`desktop.html` 计算器样式块（`.calc .disp` 之后）追加：

```css
.calc .disp.calc-break { animation: calcBreakGlow .9s ease-out 2; }
@keyframes calcBreakGlow { 0%{box-shadow:0 0 0 rgba(64,192,255,0)} 30%{box-shadow:0 0 26px rgba(64,192,255,.6)} 100%{box-shadow:0 0 0 rgba(64,192,255,0)} }
```

- [ ] **Step 3: MSG_TEXTS 加 calc_break**

`MSG_TEXTS`（约 709 行）在 `bp_dig_done` 行之后加：

```js
  calc_break:          '口令 1942 已被记录，可用于门禁。',
```

- [ ] **Step 4: 07 门禁值班员引导（pollEvents 特判）**

`pollEvents()` 在 `var map = { ... }` 声明之后、`for` 循环之前加特判（⚠️ 已按实现修正：`cz_evt_gate3_open` 不是 map 的键，写在 for-in 循环内是不可达死代码）：

```js
  /* 官网 07 门禁第一次打开 → 值班员引导破译（不埋点，值班员消息已入信息窗口） */
  if (!evtProcessed['cz_evt_gate3_open']) {
    var g3o = 0;
    try { g3o = parseInt(localStorage.getItem('cz_evt_gate3_open') || '0', 10); } catch(e){}
    if (g3o) {
      evtProcessed['cz_evt_gate3_open'] = 1;
      try { localStorage.removeItem('cz_evt_gate3_open'); } catch(e){}   // 消费后清除，防桌面刷新重复引导
      fireIncoming('老王那扇门，密码是数字。他 2009 年走的，活了 67 年——你算算。');
    }
  }
```

（⚠️ 事件写端已按实现补齐：设计文档约定官网发出 `cz_evt_gate3_open`，但原 `openGate3()` 从不写它。已在 index.html `openGate3()` 的 `logStage('gate3_open')` 之后补 1 行 `try { localStorage.setItem('cz_evt_gate3_open', Date.now()); } catch (e) {}`——与 `cz_evt_gate3_ok` 既有模式一致，新增键、未动任何既有键。**因此 Task 2 的 commit 同时含 index.html 与 desktop.html。**）

- [ ] **Step 5: 验证（Playwright 快速脚本）**

1. goto desktop.html，清除存储，设 `cz_admin_ok='1'`，reload，跳过解锁动画
2. 打开工具窗口 → 点"计算器"tab
3. 依次点击按钮文本 `2`,`0`,`0`,`9`,`−`,`6`,`7`,`=`（选择器 `.calc .k` 按 text）
4. 断言：`#cdisp` 文本为 `1942`；`#cdisp` 有类 `calc-break`；localStorage `cz_stage_log` 含 `calc_break`；系统会话收到"口令 1942 已被记录"（`#msg-list .m-sys` 最后一条）
5. 再点 `=` 一次（cExpr='1942'，1942 结果不变）→ 断言不再触发新 calc_break 日志条目（日志数不变，验证 calcBroken 防重复）
6. 端到端（⚠️ 已按实现补齐）：等官网 iframe 加载，`frame.evaluate(() => openGate3())` 真实触发 07 门禁 → 等 pollEvents 轮询 → 断言值班员引导消息入信息窗口（文案逐字、无新埋点）；重开 07 门禁断言不重复；**reload 桌面页断言引导仍不重复且 `cz_evt_gate3_open` 键已被清除**

预期：全部通过（实施时共 16 条断言）。

- [ ] **Step 6: Commit**

```bash
git add desktop.html index.html
git commit -m "门③计算器破译：2009-67 验算出 1942 → 闪光+系统消息+埋点；07门禁值班员引导

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 门④ 图纸观察 0172（desktop.html + index.html）

**Files:**
- Modify: `C:\Users\32268\Desktop\arg-game\desktop.html`（BP_B SVG 铭牌、zoom 检测、archGate01、arch01 失败提示）
- Modify: `C:\Users\32268\Desktop\arg-game\index.html`（dossier3 存档清单印证行）

**Interfaces:**
- Produces: `bpPlateSeen` 全局变量（防重复埋点）；SVG 元素 `#bp-plate`（opacity 属性切换）；`logStage('bp_plate')`；`fireMsg('bp_plate')`；arch01 密码 `0172`。MSG_TEXTS 新增 key：`bp_plate`。

> 设计文档实现微调（实施后回写设计文档第 5 节）：0172 的载体 2 印证从"桌面档案科文件"改为"官网 dossier3（地下档案室备案记录）存档清单"——dossier3 本身就是档案柜存档清单，剧情最通顺；且玩家官网过 07（1942）后才能看到，顺序自然。

- [ ] **Step 1: BP_B SVG 加柜位铭牌**

`desktop.html` `BP_B` 字符串（约 1145 行）末尾 `'</svg>'` 之前追加：

```js
  '<g id="bp-plate" opacity=".25">' +
    '<rect x="472" y="58" width="138" height="40" fill="none" stroke="#444" stroke-width="1"/>' +
    '<text x="478" y="72" font-size="9" fill="#555">柜位铭牌 · 放大查看</text>' +
    '<text x="478" y="90" font-size="14" fill="#1a1a1a" font-weight="bold">柜位 0172 · 密</text>' +
  '</g>' +
```

（位置：图纸右上角"北"箭头下方、圣器室上方的空白区。）

- [ ] **Step 2: zoom 检测触发铭牌 + 埋点**

在 `bpZoomAt()` / `bpZoomBy()` 中 `bpZoom` 更新后调用的公共刷新点（搜索 `function bpApply` 或 zoom 更新后的公共函数；若无则放 `bpZoomAt` 函数体末尾），追加：

```js
  if (!bpPlateSeen && bpZoom >= 2 && bpSvgEl && bpSvgEl.id === 'BP-B') {
    bpPlateSeen = true;
    var pl = document.getElementById('bp-plate');
    if (pl) pl.setAttribute('opacity', '1');
    fireMsg('bp_plate');
    logStage('bp_plate');
  }
```

（`bpZoomAt(1)` 重置时若 `bpPlateSeen` 已 true 则不再触发；`bpSvgEl` 为当前图纸元素，见 openBPZoom 现有变量。）

在 `var bpIsOpen = false, ...`（约 1216 行）处加：

```js
var bpPlateSeen = false;
```

`MSG_TEXTS`（`calc_break` 行之后）加：

```js
  bp_plate:            '图纸柜位铭牌信息已登记入档。',
```

- [ ] **Step 3: arch01 密码 1942 → 0172 + 失败提示**

`archGate01()`（约 1351 行）：

```js
  if (v === '0172') {
```

失败提示（`#arch01-err`，约 287 行）从"门禁密码错误。请核对王执事生平信息后重试。"改为"口令错误。放大建筑图，查看柜位铭牌。"

- [ ] **Step 4: dossier3 存档清单加 0172 印证行**

`index.html` `#dossier3` 的"存档清单"表（`<tr><td>2009-补-002</td>...` 行之后）加：

```html
          <tr><td>0172</td><td>CE-2009-001 教会建筑图纸备案（副本）</td><td style="color:#0a0;">在档</td></tr>
```

- [ ] **Step 5: 验证（Playwright 快速脚本）**

1. goto desktop.html，清存储，设 `cz_admin_ok='1'`，reload，跳过解锁
2. 内部档案 → 点 `CE-2009-001`（arch-01 详情打开）
3. 工具 → 建筑图 tab → 点"001"卡片 → `#bp-zoom.show`（⚠️ 001 卡片渲染有既有门控：需先预置 `cz_evt_blueprint_001`，或按 2 先打开 arch-01 详情）
4. 点 `＋` 按钮 4 次（1.25×/次：3 次=195% 不触发，4 次=244% ≥ 2 触发；⚠️ 已按实现修正 brief 原"3 次→2.44"的算术错误）→ 断言 `#bp-plate` 的 `opacity` 属性为 `1`，`cz_stage_log` 含 `bp_plate`
5. 关闭 bp-zoom → 档案 tab → `#arch01-input` 输 `0172` → 点解锁 → 断言 `cz_stage_log` 含 `bp_001`、`#arch01-st` 文本含"已收录"
6. `#arch01-input` 输 `1942`（旧密码）→ 应失败（`#arch01-err` 可见，`arch01-gate` 仍显示）

预期：全部通过。

- [ ] **Step 6: 回写设计文档微调**

`docs/解密手段多样化-重构设计.md` 第 5 节，将"desktop.html 档案科文件 | 弱印证：柜位索引表含 0172"改为"index.html dossier3 | 存档清单加 0172 行（印证）"。

- [ ] **Step 7: Commit**

```bash
git add desktop.html index.html docs/解密手段多样化-重构设计.md
git commit -m "门④图纸观察：BP-B 柜位铭牌 0172（放大可见）+ 档案门密码改 0172 + dossier3 印证

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 全流程测试更新（test-tracking.js）

**Files:**
- Modify: `C:\Users\32268\Desktop\arg-game\test-tracking.js`

**Interfaces:**
- Consumes: Task 2 的 `calc_break`、Task 3 的 `bp_plate` 与 `bp_001`（0172 路径）。
- Produces: 新日志总长 36；tail 12 节点；section 34 行。

- [ ] **Step 1: 重排 B 阶段**

将 B2–B3 拆为：B2 档案阅读（05/06/p1/p2 + 打开 01）→ B3 建筑图（放大看铭牌 → dig ×3）→ B3.5 档案门 0172 → B3.6 计算器破译。具体：

B3（原建筑图）改为：

```js
  console.log('[B3] 建筑图：放大看柜位铭牌 → 挖掘储物间 ×3');
  await page.dblclick('.icon[data-win="tools"]');
  await page.click('#win-tools .tab-btn:has-text("建筑图")');
  await page.locator(".bp-card[onclick*=\"openBPZoom('001')\"]").click();
  await page.waitForSelector('#bp-zoom.show');
  await page.click('#bp-zoom .bp-bar button:has-text("＋")');
  await page.click('#bp-zoom .bp-bar button:has-text("＋")');
  await page.click('#bp-zoom .bp-bar button:has-text("＋")');
  await page.click('#bp-zoom .bp-bar button:has-text("＋")');
  await waitLog('bp_plate');
  await page.click('#bp-zoom .bp-bar button:has-text("1:1")');
  for (let i = 0; i < 3; i++) {
    await page.click('#bp-zoom .bp-dig');
    await page.waitForTimeout(400);
  }
  await page.waitForSelector('#bp-reveal.on', { timeout: 5000 });
  await waitLog('dig');
  await page.click('#bp-zoom-close');
```

B2 后新增档案门与计算器：

```js
  console.log('[B3.5] 档案门：0172（图纸铭牌）');
  await page.dblclick('.icon[data-win="archive"]');
  await page.click('.arch-item:has-text("CE-2009-001")');
  await page.fill('#arch01-input', '0172');
  await page.click('#arch01-gate button');
  await waitLog('bp_001');

  console.log('[B3.6] 计算器破译：2009−67=1942');
  await page.click('#win-tools .tab-btn:has-text("计算器")');
  for (const t of ['2', '0', '0', '9', '−', '6', '7', '=']) {
    await page.click(`.calc .k:has-text("${t}")`);
  }
  await waitLog('calc_break');
```

- [ ] **Step 2: 更新断言**

`tail` 改为：

```js
  const tail = ['arch_05', 'arch_06', 'arch_p1', 'arch_p2', 'arch_01', 'bp_plate', 'dig', 'bp_001', 'calc_break', 'restore', 'handover', 'restore3'];
```

- 检查描述与值：`日志总长 = 34 条` → `36`；`日志节 32 行（交接班快照时 restore3 尚未发生：33 条 - 1）` → `日志节 34 行（交接班快照时 restore3 尚未发生：handover 前共 34 条）`（快照时日志 = 19 官网 + 5 簇 + 10 段：arch_05…restore，handover 与 restore3 均未记录 → 34 行）。

- [ ] **Step 3: 跑全流程**

```bash
cd /c/Users/32268/Desktop/arg-game
NODE_PATH=/c/Users/32268/AppData/Local/npm-cache/_npx/e41f203b7505f1fb/node_modules node test-tracking.js
```

预期：15 项断言全过（官网 19 节点顺序不变；cluster 模型不变）。

- [ ] **Step 4: Commit**

```bash
git add test-tracking.js
git commit -m "测试：四门新手段全流程（bp_plate/calc_break 节点，档案门 0172，总数 36）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 回归 + 交付

**Files:**
- Test: `C:\Users\32268\Desktop\arg-game\verify-chat.js`

- [ ] **Step 1: 联系人会话回归**

```bash
cd /c/Users/32268/Desktop/arg-game
NODE_PATH=/c/Users/32268/AppData/Local/npm-cache/_npx/e41f203b7505f1fb/node_modules node verify-chat.js
```

预期：10 项全过（确认信息窗口改造未被破坏）。

- [ ] **Step 2: git push**

```bash
git push origin main
```

- [ ] **Step 3: 汇报**

向总经理汇报：四门四手段落地、测试结果、游戏内新流程（官网 07 门禁卡住 → 桌面计算器 2009−67 → 回官网输 1942；图纸放大看铭牌 → 档案门 0172）；部署研究按总经理指示搁置。
